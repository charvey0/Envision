<div class="row d-flex justify-content-center artwork-list">
    {{#if artworks.length}}
    {{#each artworks as |artwork|}}
    <div class="card col-md-3 m-3" style="padding: 0;">
        {{!-- {{#if artwork.image_link}}
        <a href="{{artwork.image_link}}" target="_blank">
            <img class="card-img-top p-2" src="{{artwork.image_link}}" alt="Card image cap">

        </a>

        {{else}} --}}
        {{!-- <img class="card-img-top p-2" src="https://drive.google.com/thumbnail?id={{artwork.links}}" }}
            alt="Card image cap"> --}}
        <img class="card-img-top p-2" src="{{artwork.file_path}}" }} alt="Card image cap">
        {{!-- {{/if}} --}}
        <div class="card-body">


            <h5 class="card-title"><a href="/api/artwork/{{artwork.id}}" target="_blank">{{artwork.title}}</a></h5>
            <a href="/api/artwork/{{artwork.id}}" target="_blank" class="btn btn-primary">More Info</a>
            <button class="btn btn-sm btn-danger delete-artwork-btn" data-id="{{artwork.id}}">DELETE</button>
        </div>
        <div class="card-footer">
            <small class="text-muted">Submited: {{ format_date artwork.date_created}}</small>
        </div>
    </div>
    {{/each}}
    {{else}}
    <h3 class="mt-5 bg-info text-white">No artworks submited</h3>
    {{/if}}
</div>
<script src="../../delete-artwork.js"></script>